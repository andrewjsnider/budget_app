%div{ class: "max-w-xl mx-auto px-6 py-10 text-stone-900" }
  %h1{ class: "text-3xl font-semibold" } Import transactions (CSV)
  %p{ class: "mt-2 text-stone-700" }
    Upload a CSV export from your bank. Transactions will be imported into this account and categorized as “Uncategorized”.

  = form_with url: account_transactions_import_path(@account), method: :post, multipart: true, data: { turbo: false }, class: "mt-6 space-y-4" do
    %div
      %label{ class: "block text-sm text-stone-700" } CSV file
      = file_field_tag :csv_file, accept: ".csv,text/csv", class: "mt-1 w-full border border-stone-300 p-2 text-sm"

    %div{ class: "flex items-center gap-4 pt-2" }
      = submit_tag "Import", class: "border border-stone-900 px-4 py-2 text-sm hover:bg-stone-900 hover:text-white"
      = link_to "Back", account_path(@account), class: "text-sm underline text-stone-700 hover:text-stone-900"
